import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arbreapalabres.app',
  appName: 'Arbre à Palabres',
  webDir: 'build'
};

export default config;
